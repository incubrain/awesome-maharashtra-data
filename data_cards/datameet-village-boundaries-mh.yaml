---
# Awesome Marathi Datasets - Data Card
# DataMeet Indian Village Boundaries - Maharashtra

# (required) Human-readable dataset name
title: "DataMeet Indian Village Boundaries - Maharashtra"

# (required) URL to the dataset homepage, download page, or HuggingFace page
homepage: "https://projects.datameet.org/indian_village_boundaries/mh/"

# (required) Which category file this belongs to (without number prefix)
category: "geospatial-gis"

# (required) One-line description of the dataset
description: "Community-digitized village-level boundary polygons for all Maharashtra districts across 36 districts in GeoJSON format, enabling granular spatial analysis."

# (required) Data modality
modality: "geospatial"

# (required) Approximate size with units
size: "All villages across 36 districts"

# (required) ISO 639-1 language codes
language:
  - en
  - mr

# (required) License identifier
license: "ODbL-1.0"

# HuggingFace dataset URL (if available)
huggingface_url: ""

# Direct download URL (if different from homepage)
direct_download_url: ""

# Academic paper URL (arXiv, ACL Anthology, etc.)
paper_url: ""

# GitHub repository URL (if applicable)
github_url: "https://github.com/datameet/indian_village_boundaries"

# Organization or author that created/maintains the dataset
organization: "DataMeet"

# (required) List of AI/ML use cases this dataset enables
ai_use_cases:
  - "Village-level socioeconomic analysis"
  - "Census integration"
  - "Micro-level planning"
  - "Electoral mapping"

# Is this dataset already specific to Maharashtra, or does it need filtering?
maharashtra_specific: true

# Format of the data files
format: "GeoJSON"

# How frequently the data is updated (if applicable)
update_frequency: "static"

# BibTeX citation (if available)
citation: ""

# Date this data card was last verified to be accurate (YYYY-MM-DD)
last_verified: "2026-03-11"

schema:
  - field: "geometry"
    type: "Polygon"
    description: "Village boundary polygon coordinates"
  - field: "village_name"
    type: "string"
    description: "Name of the village"
  - field: "district"
    type: "string"
    description: "District the village belongs to"
  - field: "sub_district"
    type: "string"
    description: "Sub-district / taluka name"
  - field: "census_code"
    type: "string"
    description: "Census village code for cross-referencing"

starter_idea: "Build a village-level service delivery gap analyzer that overlays infrastructure data on village boundaries to identify underserved areas in Maharashtra."

build_ideas:
  - "Create a granular election results visualization tool mapping party-wise results to individual village polygons across Maharashtra"
  - "Develop a school catchment area optimizer that assigns students to nearest schools using village boundary and road network data"
  - "Build a crop insurance claims mapper that visualizes damage reports at village level to detect patterns and potential fraud"

quick_start: |
  import geopandas as gpd
  # Download from https://github.com/datameet/indian_village_boundaries
  gdf = gpd.read_file('mh/maharashtra_village_boundaries.geojson')
  print(f"Total villages: {len(gdf)}")
  print(f"Districts: {gdf['district'].nunique()}")
  print(gdf[['village_name', 'district', 'sub_district']].head())
