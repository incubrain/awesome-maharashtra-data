---
# Awesome Marathi Datasets - Data Card
# Bhuvan NRSC Open EO Data Archive

# (required) Human-readable dataset name
title: "Bhuvan NRSC Open EO Data Archive"

# (required) URL to the dataset homepage, download page, or HuggingFace page
homepage: "https://bhuvan.nrsc.gov.in/ngmaps"

# (required) Which category file this belongs to (without number prefix)
category: "geospatial-gis"

# (required) One-line description of the dataset
description: "ISRO satellite imagery archive including Resourcesat LISS-III, AWiFS, and Cartosat DEM freely downloadable for the Maharashtra region, with resolutions from 23.5m to 56m."

# (required) Data modality
modality: "geospatial"

# (required) Approximate size with units
size: "20+ TB archive; Cartosat DEM 30m, LISS-III 23.5m, AWiFS 56m resolution"

# (required) ISO 639-1 language codes
language:
  - en

# (required) License identifier
license: "Government-Open"

# HuggingFace dataset URL (if available)
huggingface_url: ""

# Direct download URL (if different from homepage)
direct_download_url: "https://bhuvan-app3.nrsc.gov.in/data/"

# Academic paper URL (arXiv, ACL Anthology, etc.)
paper_url: ""

# GitHub repository URL (if applicable)
github_url: ""

# Organization or author that created/maintains the dataset
organization: "ISRO / National Remote Sensing Centre (NRSC)"

# (required) List of AI/ML use cases this dataset enables
ai_use_cases:
  - "Crop mapping"
  - "NDVI time-series analysis"
  - "DEM-based flood modeling"
  - "Change detection"

# Is this dataset already specific to Maharashtra, or does it need filtering?
maharashtra_specific: false

# Format of the data files
format: "GeoTIFF"

# How frequently the data is updated (if applicable)
update_frequency: "monthly"

# BibTeX citation (if available)
citation: ""

# Date this data card was last verified to be accurate (YYYY-MM-DD)
last_verified: "2026-03-11"

schema:
  - field: "tile_id"
    type: "string"
    description: "Unique tile/scene identifier for the satellite image"
  - field: "acquisition_date"
    type: "date"
    description: "Date the satellite image was captured"
  - field: "sensor"
    type: "string"
    description: "Sensor type (LISS-III, AWiFS, Cartosat)"
  - field: "resolution_m"
    type: "float"
    description: "Spatial resolution in meters"
  - field: "bands"
    type: "list[string]"
    description: "Spectral bands available (e.g., Green, Red, NIR, SWIR)"

starter_idea: "Build a crop health monitoring system using NDVI time-series from LISS-III imagery to detect drought stress in Maharashtra's rainfed agriculture zones."

build_ideas:
  - "Create an automated flood extent mapper for Maharashtra using pre/post monsoon satellite imagery with change detection algorithms"
  - "Develop a deforestation monitoring dashboard for Western Ghats using multi-temporal satellite data with deep learning classification"
  - "Build a solar farm site suitability model combining Cartosat DEM with land cover classification from LISS-III imagery"

quick_start: |
  # Register at https://bhuvan.nrsc.gov.in/ for data access
  import rasterio
  import numpy as np
  # After downloading a LISS-III GeoTIFF for Maharashtra:
  # src = rasterio.open('liss3_maharashtra.tif')
  # nir, red = src.read(4), src.read(3)
  # ndvi = (nir.astype(float) - red) / (nir + red + 1e-10)
  # print(f"NDVI range: {ndvi.min():.2f} to {ndvi.max():.2f}")
  print("Register at https://bhuvan.nrsc.gov.in/ to download GeoTIFF tiles")
